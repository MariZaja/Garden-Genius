# --- Code generated by ChatGPT-4o ---
version: '3.8'

services:
  mongodb:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: garden-genius-mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: garden
      MONGO_INITDB_DATABASE: gardengeniusdb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - mongo-network

volumes:
  mongo-data:

networks:
  mongo-network:

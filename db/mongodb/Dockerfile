# --- Code generated by ChatGPT-4o ---

# Use the official MongoDB image as a base
FROM mongo:latest

# Set environment variables (optional)
ENV MONGO_INITDB_ROOT_USERNAME=admin
ENV MONGO_INITDB_ROOT_PASSWORD=examplepassword
ENV MONGO_INITDB_DATABASE=mydatabase

# Expose the default MongoDB port
EXPOSE 27017

# Copy any initialization scripts (if needed)
# ADD init-mongo.js /docker-entrypoint-initdb.d/

# The CMD command is already defined in the base image, so no need to override it unless you have a specific command
# CMD ["mongod"]

# Optional: Run additional commands
# RUN some-command

# Docker will automatically use the default CMD provided by the base image to start MongoDB
